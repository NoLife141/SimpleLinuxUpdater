# Copy this file to .env and update values before running Docker.

# Optional: session cookie hardening (recommended true behind HTTPS reverse proxy)
# DEBIAN_UPDATER_SESSION_COOKIE_SECURE=false
# Set DEBIAN_UPDATER_SESSION_COOKIE_SECURE=true for HTTPS deployments.

# Optional: idle timeout for web sessions in hours (0/unset keeps default behavior)
# DEBIAN_UPDATER_SESSION_IDLE_TIMEOUT_HOURS=0

# Optional: custom DB path inside the container
# DEBIAN_UPDATER_DB_PATH=/data/servers.db

# Optional: override known_hosts paths inside the container
# Default is /data/known_hosts (persisted with the volume)
# DEBIAN_UPDATER_KNOWN_HOSTS=/data/known_hosts:/etc/ssh/ssh_known_hosts

# Optional: retry policy for transient SSH/network/APT failures
# DEBIAN_UPDATER_RETRY_MAX_ATTEMPTS=3
# DEBIAN_UPDATER_RETRY_BASE_DELAY_MS=1000
# DEBIAN_UPDATER_RETRY_MAX_DELAY_MS=8000
# DEBIAN_UPDATER_RETRY_JITTER_PCT=20
# Optional: per-command SSH execution timeout in seconds (default 300)
# DEBIAN_UPDATER_SSH_COMMAND_TIMEOUT_SECONDS=300

# Optional: trusted reverse proxies for real client IP handling (comma-separated IPs/CIDRs)
# Leave unset/none to trust no proxies.
# DEBIAN_UPDATER_TRUSTED_PROXIES=127.0.0.1,10.0.0.0/8

# Optional: post-update health checks
# DEBIAN_UPDATER_POSTCHECKS_ENABLED=true
# DEBIAN_UPDATER_POSTCHECK_BLOCK_ON_APT_HEALTH=true
# DEBIAN_UPDATER_POSTCHECK_BLOCK_ON_FAILED_UNITS=true
# DEBIAN_UPDATER_POSTCHECK_REBOOT_REQUIRED_WARNING=true
# DEBIAN_UPDATER_POSTCHECK_CMD=

# Programmatic auth endpoints (/api/auth/setup, /api/auth/login, /api/auth/logout)
# require host-matching same-origin headers:
#   Origin: http://localhost:8080 (or your UI origin)
#   Referer: http://localhost:8080/ (or your UI page URL)
#   Sec-Fetch-Site: same-origin (recommended; enforced only if present)
