# Documentation Archive

This folder keeps historical planning, refactor, and one-time validation records that are useful for project archaeology but are no longer active user or contributor documentation.

Active documentation remains in `docs/` and is linked from the main README. Archived files should not be used as current implementation guidance without checking the current code first.
